import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.react.vault.manager',
  appName: 'React Vault Manager',
  webDir: 'build/mobile',
};

export default config;
